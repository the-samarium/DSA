#include<iostream>
using namespace std;
/*
You are given 3 rods (A, B, C) and n disks of different sizes stacked on rod A (largest at bottom, smallest on top).

Goal:
Move all disks from rod A → rod C, using rod B as a helper, following these rules:
Only one disk can be moved at a time.
You can only move the top disk of any rod.

No larger disk can be placed on a smaller disk.

CONTINUE OF SATURDAY:11TH OCT
*/
int main(){
    return 0;
}